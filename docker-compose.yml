version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: iot-monitor-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - iot-network

networks:
  iot-network:
    driver: bridge
